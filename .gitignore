.env
*.pyc
__pycache__/
.venv/
.pytest_cache/

# OpenWA oturum verisi ve medya — asla repoya girmez
openwa-data/

# Hermes CLI kaldırıldı (kendi in-process ajanımız var, bkz. app/ajan.py).
# hermes-home/ klasörü artık yalnız SOUL.md'yi (ajanın kimliği) barındırıyor,
# gerisi eski CLI'nin çalışma verisi — logs/ içinde hasta mesajları geçebilir,
# repoya asla girmemeli (KVKK).
hermes-home/*
!hermes-home/SOUL.md

# Referans zip'ler (inşada kullanılmıyor)
referans/
